/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package dsa.sheet.app;

import dsa.sheet.list.LinkedList;

import static dsa.sheet.utilities.StringUtils.join;
import static dsa.sheet.utilities.StringUtils.split;
import static dsa.sheet.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
